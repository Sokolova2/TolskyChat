- @contacts.each do |contact|
  - if contact.sender_id == current_user.id || contact.receiver_id == current_user.id
    - user = (contact.sender_id == current_user.id ? contact.receiver : contact.sender)
    .d-flex.gap-2
      = image_tag user.avatar.url(:thumb), size: 25
      %p= user.login


