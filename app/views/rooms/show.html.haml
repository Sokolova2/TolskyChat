= render 'navbar/navbar'

.page-bg
  .rooms-layout
    .rooms-sidebar.d-none.d-lg-block
      = render 'navbar/room_list', rooms: @rooms
    .room-wrapper.room-wrapper--show
      = render 'rooms/show_room', room: @room
